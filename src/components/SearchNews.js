import React from "react";
import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
 
});

export const SearchNews = () => {
    //const classes = useStyles();
    return <div>Searc News</div>
}   